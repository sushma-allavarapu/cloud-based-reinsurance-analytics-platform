version: 2

models:
  - name: dim_customer

    columns:
      - name: customer_id
        tests:
          - unique
          - not_null

      - name: customer_name
        tests:
          - not_null
  - name: dim_policy

    columns:
      - name: policy_id
        tests:
          - unique
          - not_null
  - name: dim_benefit

    columns:
      - name: benefit_id
        tests:
          - unique
          - not_null
  - name: dim_insurance_company

    columns:
      - name: insurance_company_id
        tests:
          - unique
          - not_null
  - name: dim_reinsurance_company

    columns:
      - name: reinsurance_company_id
        tests:
          - unique
          - not_null